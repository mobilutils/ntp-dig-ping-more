# HTTPS Certificate Inspector — Tasks

- [x] Create `HttpsCertRepository.kt` (data model + TLS logic)
- [x] Create `HttpsCertViewModel.kt`
- [x] Create `HttpsCertScreen.kt`
- [x] Modify `MainActivity.kt` (route + NavHost + bottom nav)
- [x] Modify `MoreToolsScreen.kt` (add to extra tools list)
- [x] Build verification (`./gradlew assembleDebug`) — ✅ BUILD SUCCESSFUL
